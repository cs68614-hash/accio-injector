{
  "name": "accio-injector-poc",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Proof-of-concept Electron loader/preload injection for a copied Accio.app bundle.",
  "scripts": {
    "inject": "node src/inject.mjs",
    "verify": "node src/verify.mjs"
  },
  "dependencies": {
    "@electron/asar": "^3.4.0",
    "plist": "^3.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
